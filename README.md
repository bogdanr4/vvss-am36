# Repository Lab VVSS - Echipa AM36

## Rulare

Pentru rulare se creează o configurare în IntelliJ:

`Run/Debug Configurations > Edit configurations... > Add New Configuration > Maven`, apoi
câmpul `Run` se completează cu `javafx:run`, apoi `Apply`.
